# Code

- Follow the `vibe-research-coding` skill (general best practices) and consult `my-scientific-taste` (see the division-of-labor table in the root CLAUDE.md).
- Every tidiness requirement here rests on one premise: **the user reads all of this code**. None of it applies to `data/` or `tmp/`.

## Project-specific rules and lessons

(Accumulates as the project runs; add entries as "rule + reason + scope".)
